<Window x:Class="Sensing4U.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Sensing4U Dashboard" Height="500" Width="900">

    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top">
            <Menu Height="25">
                <MenuItem Header="_File">
                    <MenuItem Header="Load Binary" Click="LoadBin_Click"/>
                    <MenuItem Header="Load CSV" Click="LoadCsv_Click"/>
                    <Separator/>
                    <MenuItem Header="Save Binary" Click="SaveBin_Click"/>
                    <MenuItem Header="Save CSV" Click="SaveCsv_Click"/>
                </MenuItem>
            </Menu>

            <StackPanel Orientation="Horizontal" Margin="0,10">
                <Label Content="Search (Label):" VerticalAlignment="Center"/>
                <TextBox x:Name="txtSearch" Width="100" Margin="5,0"/>
                <Button Content="Search" Click="BtnSearch_Click" Width="70"/>

                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0"/>

                <Label Content="Lower:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtLower" Width="50" Text="10" VerticalAlignment="Center"/>
                <Label Content="Upper:" VerticalAlignment="Center"/>
                <TextBox x:Name="txtUpper" Width="50" Text="50" VerticalAlignment="Center"/>
                <Button Content="Set Bounds" Click="UpdateThresholds_Click" Margin="5,0"/>
            </StackPanel>
        </StackPanel>

        <StatusBar DockPanel.Dock="Bottom" Background="LightGray">
            <StatusBarItem>
                <TextBlock Text="Average Value: "/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock x:Name="txtAverage" Text="0.00" FontWeight="Bold"/>
            </StatusBarItem>
        </StatusBar>

        <DataGrid x:Name="SensorGrid" 
                  AutoGenerateColumns="True" 
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  LoadingRow="SensorGrid_LoadingRow"
                  Margin="0,5"/>
    </DockPanel>
</Window>